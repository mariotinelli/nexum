{
  "schema_version": 1,
  "scenarios": [
    {
      "id": "human-readable-adaptation",
      "input": "Inspection confirms existing login rejects a supported profile. The draft merely promises login for all profiles and combines missing fields, invalid credentials and inactive accounts into one error criterion.",
      "expect": ["explain confirmed current limitation and intended change in delivery", "state permissions to preserve", "separate independently verifiable scenarios", "preserve every assigned condition", "avoid implementation recipe and internal category labels"]
    },
    {
      "id": "standalone-task-relations",
      "input": "A draft refers to delivery 1 and says shared code is not a start blocker, while integration is required to verify deactivation. The related child is not published yet.",
      "expect": ["identify related task by name without invented remote ID", "distinguish start blocker from completion integration", "remove workflow commentary", "do not silently modify approved descriptions to add links during publication"]
    },
    {
      "id": "qa-execution-focused-writing",
      "input": "QA puts its estimate in the objective, tech lead approval policy under impacts, and remember-me restoration and expiration in one long journey.",
      "expect": ["keep objective short and estimate in native field", "split journeys by verifiable behavior with required setup", "keep impacts about product behavior", "preserve full parent coverage including existing behavior", "keep workflow approval administration out of task description"]
    },
    {
      "id": "installed-default-differs-from-assumption",
      "input": "A proposal calls remembered access indefinite, but the installed dependency defines a finite duration and project overrides may change it.",
      "expect": ["inspect installed version and project overrides", "cite actual value with unit and conditions", "distinguish inspected evidence from executed verification", "record unavailable effective configuration as uncertainty without requesting a product decision"]
    },
    {
      "id": "partial-existing-behavior",
      "input": "A requirement demands revocation on the next action in every connected device; inspection only identifies a soft-delete mechanism and a single-session logout test.",
      "expect": ["follow relevant authentication paths before declaring existing coverage", "test presence does not prove untested scenarios", "confirmed missing behavior becomes adapt with a slice for the gap", "inconclusive evidence remains explicit uncertainty", "no DEV task solely because a test is absent"]
    },
    {
      "id": "traceability-with-missing-acceptance-conditions",
      "input": "A slice maps an accessibility criterion requiring desktop/mobile use, keyboard navigation and visible focus, but its description only promises accessible feedback.",
      "expect": ["reject semantic coverage based solely on requirement IDs", "express assigned observable conditions in acceptance criteria before approval", "keep descriptions human-readable without prescribing implementation"]
    },
    {
      "id": "qa-covers-existing-behavior",
      "input": "A publication adds one DEV slice while logout and cross-device deactivation are confirmed existing behavior in the parent requirement.",
      "expect": ["integrated QA includes existing logout and deactivation scenarios", "preserve approved device and session conditions", "no artificial DEV work for confirmed existing behavior"]
    },
    {
      "id": "independent-profile-actions",
      "input": "A profile Feature whose view and password-change behaviors can be verified independently.",
      "expect": ["independent vertical slices", "no artificial layer task", "complete coverage"]
    },
    {
      "id": "cross-feature-integration-owner",
      "input": "Project, task, and time Features share an integration point owned by one approved Feature.",
      "expect": ["one delivery owner", "specific blockers", "no duplicated integration"]
    },
    {
      "id": "unsliced-external-feature",
      "input": "A DEV slice cannot start without a capability owned by an external Feature that has no children yet.",
      "expect": ["explicit provisional parent blocker", "blocking capability and reason", "later specific refinement"]
    },
    {
      "id": "external-feature-now-sliced",
      "input": "A previously provisional external Feature now has DEV children and the relevant graph contains other blocking edges.",
      "expect": ["tech-lead-approved exact replacement", "complete-graph cycle rejection", "unrelated relations preserved"]
    },
    {
      "id": "approved-api-delivery",
      "input": "The Feature explicitly promises a consumable API.",
      "expect": ["API marker after WEB", "API behavior is verifiable", "no scope expansion"]
    },
    {
      "id": "internal-endpoint",
      "input": "A WEB behavior needs an internal endpoint but the Feature does not promise an API product.",
      "expect": ["no API marker", "endpoint remains inside the vertical behavior"]
    },
    {
      "id": "unapproved-api-expansion",
      "input": "The proposed slices add a consumable API although the approved parent promises only WEB behavior.",
      "expect": ["material divergence", "exact parent correction and affected children", "no child publication before tech-lead decision and verified parent alignment", "fresh ordinary approvals after alignment"]
    },
    {
      "id": "already-implemented",
      "input": "Inspection proves every approved behavior already exists.",
      "expect": ["coverage cites evidence", "empty DEV slice set", "versioned tech-lead confirmation", "QA-only handoff"]
    },
    {
      "id": "existing-partial-child-set",
      "input": "The parent has one reusable DEV child, one external child, and one uncovered behavior.",
      "expect": ["all existing children presented", "explicit adopt or external decision", "only uncovered DEV work proposed", "stable adopted ID"]
    },
    {
      "id": "small-feature",
      "input": "All behavior forms one cohesive implementation under the six-hour reference.",
      "expect": ["one DEV slice is valid", "no artificial split"]
    },
    {
      "id": "blocking-technical-uncertainty",
      "input": "A precise concurrency uncertainty prevents a complete vertical decomposition.",
      "expect": ["bounded Study question", "attributed source-bound result", "new proposal revision", "ordinary approval repeated", "no invented DEV or QA children"]
    },
    {
      "id": "editorial-requirement-revision",
      "input": "The approved requirement has formatting, spelling, and wording changes but retains every observable behavior and rule.",
      "expect": ["editorial classification based on meaning", "recorded byte/version change", "all tasks and coverage remain valid", "no re-slicing"]
    },
    {
      "id": "localized-functional-revision",
      "input": "One approved behavior changes after DEV and QA children have been published, while other behavior and dependencies remain valid.",
      "expect": ["human-readable affected and unaffected inventory", "specific tech-lead approval", "stable unaffected remote IDs", "no whole-set recreation"]
    },
    {
      "id": "revision-across-task-states",
      "input": "A localized approved revision affects not-started, in-progress, and completed work and one DEV-to-QA blocker.",
      "expect": ["fresh native status readback", "task-bound approval for in-progress mutation", "new child for completed history", "approved cancellation reason and replacements", "unrelated relations preserved"]
    },
    {
      "id": "uncertain-revision-application",
      "input": "Create, update, cancel, relation-add, or relation-remove returns an uncertain result while applying an approved revision.",
      "expect": ["persisted intent", "readback before retry", "append-only observation", "no duplicate remote work", "final preservation proof"]
    }
  ]
}
