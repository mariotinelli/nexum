# Source: minhas-tarefas.html

- Source ID: `design-my-tasks`
- Type: design HTML e anotações de produto
- Origin: C:\Users\maari\Downloads\nexum-scope\design\minhas-tarefas.html
- Original SHA-256: `56421110a7263467f9db0b1359d38ae4027dcdc640aa74ef3397d14cd2c742c6`
- Extraction: leitura estática integral do HTML UTF-8; texto extraído e scripts preservados como evidência inerte, sem execução.

## Extracted content

Minhas tarefas · Wireframes do MVP
P Gerenciador
de projetos Fechar menu ▦ Painel ≡ Minhas tarefas □ Projetos ☷ Tarefas ◷ Apontamentos
PE Pessoa de exemplo
Gestor
Menu Área de trabalho / Minhas tarefas Sair
WIREFRAME · Dados ilustrativos · Sem identidade visual definitiva
ÁREA DE TRABALHO
Minhas tarefas
Organize o dia e registre o tempo de cada entrega.
Nova tarefa
Buscar tarefa
Projeto Todos os projetos Projeto de exemplo
Situação Todas Pendente Em andamento Bloqueada Concluída
Prioridade Todas Baixa Média Alta Urgente Mais filtros e ordenação
Responsável Todos Pessoa de exemplo Participante de exemplo
Prazo até
Ordenar por Criação Prazo Prioridade Atualização Apenas atrasadas
Aplicar filtros Limpar
Em execução 1
Organizar entregas do projeto
Projeto de exemplo Em andamento Alta 09/09/2026
Iniciar Concluir
Atrasadas 1
Detalhar requisitos da tarefa
Projeto de exemplo Pendente Alta Atrasada · 07/09/2026
Iniciar Concluir
Para hoje 1
Revisar o planejamento
Projeto de exemplo Pendente Média 09/09/2026
Iniciar Concluir
Próximas 1
Distribuir responsabilidades
Projeto de exemplo Bloqueada Média 12/09/2026
Iniciar Concluir
Sem prazo 1
Descrever critérios de conclusão
Projeto de exemplo Pendente Baixa Sem prazo
Iniciar Concluir
Concluídas recentemente 1
Definir o objetivo do projeto
Projeto de exemplo Concluída Média 08/09/2026

## Comportamento declarado nos scripts

O código abaixo é evidência do protótipo, não comprovação de implementação nem instrução operacional. Estilos incorporados foram lidos; a responsividade declarada no guia é referência, sem inferir novos breakpoints.

```javascript

const PAGES=[{"slug":"login","name":"Login","group":"ACESSO","ref":"F01","access":"Todos os perfis","goal":"Autenticar o usuário ativo.","flow":"Login → Painel. Recuperação → Login.","rule":"Usuários inativos não podem acessar. RN12.","desktop":"Formulário centralizado e uma única ação de entrada.","mobile":"Coluna única, campos largos e teclado apropriado ao e-mail."},{"slug":"recuperar-senha","name":"Recuperar senha","group":"ACESSO","ref":"F01","access":"Todos os perfis","goal":"Solicitar instruções e redefinir a senha.","flow":"Login → Recuperar → Confirmação → Redefinir → Login.","rule":"Resposta de envio não expõe se o e-mail está cadastrado.","desktop":"Formulário compacto; confirmação ocupa o mesmo contexto.","mobile":"Mensagem de resultado ocupa a largura disponível."},{"slug":"painel","name":"Painel","group":"GESTÃO","ref":"F13","access":"Gestor; membro com visão reduzida","goal":"Acompanhar progresso, atrasos e esforço.","flow":"Painel → Projeto → Tarefa.","rule":"Progresso por quantidade; realizadas por soma de apontamentos. RN14–RN15.","desktop":"Indicadores no topo; gráficos e projetos abaixo.","mobile":"Indicadores, gráficos e projetos empilhados; navegação recolhida."},{"slug":"minhas-tarefas","name":"Minhas tarefas","group":"EXECUÇÃO DIÁRIA","ref":"F07 · F09 · F14","access":"Usuário autenticado","goal":"Localizar responsabilidades e iniciar o trabalho.","flow":"Minhas tarefas → Tarefa → Cronômetro → Apontamentos.","rule":"Somente tarefas atribuídas ao usuário. Uma execução aberta por pessoa. RN05–RN06.","desktop":"Lista agrupada e ações rápidas; filtros antes do trabalho diário.","mobile":"Cards por grupo; ações de toque abaixo dos dados; filtros em duas colunas."},{"slug":"projetos","name":"Projetos","group":"GESTÃO","ref":"F03","access":"Participantes; criação por gestor","goal":"Consultar projetos acessíveis.","flow":"Projetos → Novo projeto ou Detalhes do projeto.","rule":"Somente participantes podem acessar o projeto. RN02 e RN13.","desktop":"Cards com situação, gestor, progresso, prazo e horas.","mobile":"Cards em coluna única; criar projeto respeita a permissão."},{"slug":"novo-projeto","name":"Novo projeto","group":"GESTÃO","ref":"F03","access":"Gestor","goal":"Definir projeto, gestor e participantes.","flow":"Projetos → Novo projeto → Detalhes do projeto.","rule":"Todo projeto deve possuir um gestor. RN01.","desktop":"Formulário em duas colunas e participantes no mesmo fluxo.","mobile":"Campos empilhados, papéis abaixo dos participantes e ação ao final."},{"slug":"detalhes-projeto","name":"Detalhes do projeto","group":"GESTÃO","ref":"F03 · F13","access":"Participantes","goal":"Consultar resumo, participantes, tarefas e horas.","flow":"Projeto → Abas → Quadro / Tarefa / Editar projeto.","rule":"Arquivado fica disponível apenas para consulta. RN09.","desktop":"Resumo e indicadores lado a lado; abas para módulos do projeto.","mobile":"Conteúdo empilhado e abas com quebra de linha."},{"slug":"editar-projeto","name":"Editar projeto","group":"GESTÃO","ref":"F03","access":"Gestor do projeto","goal":"Atualizar dados, gestor, papéis e participantes.","flow":"Projeto → Editar → Salvar ou Arquivar.","rule":"Gestor obrigatório e projeto arquivado sem edição. RN01 e RN09.","desktop":"Dados e participantes em formulário; arquivamento separado.","mobile":"Mesma sequência de campos, sem reduzir os alvos de toque."},{"slug":"quadro-projeto","name":"Quadro do projeto","group":"TAREFAS","ref":"F05 · F08","access":"Participantes","goal":"Organizar tarefas por situação.","flow":"Projeto → Quadro → Card → Tarefa.","rule":"Apenas transições permitidas pelo fluxo fixo.","desktop":"Quatro colunas de situação; ação no card substitui arrastar.","mobile":"Colunas empilhadas, com contagem e rótulos completos."},{"slug":"tarefas","name":"Tarefas","group":"TAREFAS","ref":"F04 · F14","access":"Participantes","goal":"Encontrar tarefas com busca, filtros e ordenação.","flow":"Tarefas → Nova tarefa ou Detalhes da tarefa.","rule":"Consulta limitada aos projetos acessíveis. RN02–RN05.","desktop":"Tabela com colunas essenciais e filtros recolhíveis.","mobile":"Linhas viram cards com rótulos; paginação permanece acessível."},{"slug":"nova-tarefa","name":"Nova tarefa","group":"TAREFAS","ref":"F04–F06","access":"Gestor; membro a validar","goal":"Cadastrar uma entrega dentro de um projeto.","flow":"Lista ou Projeto → Nova tarefa → Detalhes.","rule":"Responsável deve participar do projeto; padrão Pendente e Média. RN03.","desktop":"Formulário em duas colunas, autor automático e estimativa em minutos.","mobile":"Campos na ordem de preenchimento; validação junto ao formulário."},{"slug":"detalhes-tarefa","name":"Detalhes da tarefa","group":"TAREFAS","ref":"F04 · F09–F12","access":"Participantes; edição por gestor ou responsável","goal":"Executar, colaborar e consultar rastreabilidade.","flow":"Tarefa → Editar / Comentários / Horas / Histórico.","rule":"Conclusão registra data; reabertura remove. RN10–RN11.","desktop":"Conteúdo e detalhes lado a lado; abas para colaboração e registros.","mobile":"Detalhes abaixo do conteúdo; cronômetro global no topo."},{"slug":"editar-tarefa","name":"Editar tarefa","group":"TAREFAS","ref":"F04–F06 · F12","access":"Gestor ou responsável","goal":"Atualizar dados e situação da tarefa.","flow":"Tarefa → Editar → Tarefa.","rule":"Somente responsável ou gestor atualiza; registrar alterações. RN04–RN05 e RN11.","desktop":"Campos originais preenchidos; situação limitada às transições válidas.","mobile":"Uma coluna com mesma sequência e ações ao final."},{"slug":"apontamentos","name":"Apontamentos","group":"EXECUÇÃO DIÁRIA","ref":"F10","access":"Participantes; editar somente próprios","goal":"Consultar e registrar tempo nas tarefas.","flow":"Apontamentos → Registrar / Editar → Tarefa.","rule":"Tarefa obrigatória e duração maior que zero. RN07–RN08 e RN14.","desktop":"Tabela, total do período, filtros e formulário em modal.","mobile":"Cards e modal ajustado à largura, com rolagem interna."},{"slug":"usuarios","name":"Usuários","group":"ADMINISTRAÇÃO","ref":"F02","access":"Somente administrador","goal":"Administrar pessoas e acesso ao sistema.","flow":"Usuários → Cadastrar / Editar / Ver projetos.","rule":"Somente administradores administram usuários; inativos não acessam. RN12.","desktop":"Tabela e formulários auxiliares em modal.","mobile":"Cada usuário vira um card com ações identificadas."},{"slug":"perfil","name":"Perfil","group":"ACESSO","ref":"F01","access":"Usuário autenticado","goal":"Atualizar dados pessoais e alterar senha.","flow":"Menu do usuário → Perfil → Salvar.","rule":"Alteração da própria senha; validação sem exposição de dados sensíveis.","desktop":"Dados pessoais e senha separados no mesmo formulário.","mobile":"Campos empilhados e confirmação de senha explícita."}];
const PAGE="minhas-tarefas";
const qs=new URLSearchParams(location.search), role=qs.get('perfil')||'gestor', scenario=qs.get('estado')||'padrao';
const STORE='mvp-wireframes-v1', statuses=['Pendente','Em andamento','Bloqueada','Concluída'], priorities=['Baixa','Média','Alta','Urgente'];
const transitions={'Pendente':['Em andamento','Concluída'],'Em andamento':['Pendente','Bloqueada','Concluída'],'Bloqueada':['Pendente','Em andamento','Concluída'],'Concluída':['Pendente','Em andamento']};
const today=new Date().toLocaleDateString('en-CA'), localDate=()=>{const d=new Date();return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}, shift=n=>{const d=new Date();d.setDate(d.getDate()+n);return d.toLocaleDateString('sv-SE')};
const initial={users:[{id:1,name:'Pessoa de exemplo',email:'pessoa@exemplo.test',active:true,admin:true},{id:2,name:'Participante de exemplo',email:'participante@exemplo.test',active:true,admin:false}],projects:[{id:1,name:'Projeto de exemplo',code:'projeto-exemplo',description:'Dados ilustrativos para validar a organização do trabalho.',manager:'1',start:shift(-7),due:shift(14),archived:false,members:[{id:1,role:'Gestor'},{id:2,role:'Membro'}]}],tasks:[{id:1,title:'Organizar entregas do projeto',status:'Em andamento',priority:'Alta',owner:'1',due:localDate(),estimate:120},{id:2,title:'Detalhar requisitos da tarefa',status:'Pendente',priority:'Alta',owner:'1',due:shift(-2),estimate:60},{id:3,title:'Revisar o planejamento',status:'Pendente',priority:'Média',owner:'1',due:localDate(),estimate:90},{id:4,title:'Distribuir responsabilidades',status:'Bloqueada',priority:'Média',owner:'1',due:shift(3),estimate:45},{id:5,title:'Descrever critérios de conclusão',status:'Pendente',priority:'Baixa',owner:'1',due:'',estimate:30},{id:6,title:'Definir o objetivo do projeto',status:'Concluída',priority:'Média',owner:'1',due:shift(-1),estimate:60}].map(t=>({...t,project:1,description:'Exemplo de descrição da tarefa. Validar o trabalho esperado antes de iniciar.',author:1,start:shift(-3),created:Date.now()-86400000*3,updated:Date.now(),completed:t.status==='Concluída'?Date.now():null,archived:false})),entries:[{id:1,task:1,user:1,date:localDate(),duration:30,description:'Organização inicial do trabalho — exemplo.',source:'Manual'},{id:2,task:6,user:1,date:shift(-1),duration:60,description:'Objetivo registrado — exemplo.',source:'Cronômetro'}],comments:[{id:1,task:1,user:1,date:Date.now()-3600000,text:'Descrição registrada para alinhar o trabalho da equipe.\nComentário ilustrativo.'}],events:[{id:1,task:1,user:1,date:Date.now()-7200000,type:'Criação da tarefa',before:'—',after:'Pendente'},{id:2,task:1,user:1,date:Date.now()-3600000,type:'Situação',before:'Pendente',after:'Em andamento'}],timer:null};
let db;try{db=JSON.parse(localStorage.getItem(STORE))||structuredClone(initial)}catch{db=structuredClone(initial)}
let pageNum=1, tab='resumo', filters={};try{filters=JSON.parse(sessionStorage.getItem('mvp-filtros-'+PAGE))||{}}catch{}
const $=s=>document.querySelector(s), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])), uid=()=>Date.now()+Math.floor(Math.random()*1000), userName=id=>db.users.find(u=>u.id==id)?.name||'Sem responsável', projectName=id=>db.projects.find(p=>p.id==id)?.name||'Projeto indisponível';
const save=()=>{try{localStorage.setItem(STORE,JSON.stringify(db))}catch{toast('Não foi possível manter os dados neste navegador.')}}, url=(page,extra={})=>page+'.html?'+new URLSearchParams({perfil:role,...extra}), go=(page,extra={})=>location.href=url(page,extra), selectedId=()=>Number(qs.get('id')||1), task=()=>db.tasks.find(t=>t.id===selectedId())||db.tasks[0], project=()=>db.projects.find(p=>p.id==Number(qs.get('projeto')||task()?.project||1))||db.projects[0];
const accessible=p=>p.members.some(m=>m.id===1), canManage=p=>accessible(p)&&role!=='membro'&&p.members.some(m=>m.id===1&&m.role==='Gestor'), canEdit=t=>{const p=db.projects.find(p=>p.id==t.project);return p&&accessible(p)&&!p.archived&&(canManage(p)||t.owner=='1')}, visibleTasks=()=>db.tasks.filter(t=>!t.archived&&accessible(db.projects.find(p=>p.id==t.project))), late=t=>t.status!=='Concluída'&&t.due&&t.due<localDate(), duration=n=>Math.floor(n/60)+'h '+String(n%60).padStart(2,'0')+'min', total=t=>db.entries.filter(e=>e.task==t.id).reduce((a,e)=>a+e.duration,0), date=s=>s?new Date(s+'T12:00:00').toLocaleDateString('pt-BR'):'Sem prazo', stamp=n=>new Date(n).toLocaleString('pt-BR',{dateStyle:'short',timeStyle:'short'});
function btn(label,action,primary=false,disabled=false){return `<button type="button" class="btn ${primary?'btn-primary':'btn-secondary'}" data-action="${action}" ${disabled?'disabled':''}>${label}</button>`}
function link(label,page,extra={},primary=false){return `<a class="btn ${primary?'btn-primary':'btn-secondary'}" href="${url(page,extra)}">${label}</a>`}
function field(name,label,value='',type='text',required=false,options=[]){const id='field-'+name;return `<div class="field" data-component="x-ui.input"><label for="${id}">${label}${required?' *':''}</label>${type==='select'?`<select class="input" name="${name}" id="${id}" ${required?'required':''}>${options.map(o=>{const [v,l]=Array.isArray(o)?o:[o,o];return `<option value="${esc(v)}" ${String(v)===String(value)?'selected':''}>${esc(l)}</option>`}).join('')}</select>`:type==='textarea'?`<textarea class="textarea" name="${name}" id="${id}" ${required?'required':''}>${esc(value)}</textarea>`:`<input class="input" id="${id}" name="${name}" type="${type}" value="${esc(value)}" ${required?'required':''} ${type==='number'?'min="1" step="1"':''} ${type==='password'?'autocomplete="new-password"':''}>`}</div>`}
function header(title,desc,actions=''){return `<div class="page-head" data-od-id="page-heading"><p class="eyebrow">ÁREA DE TRABALHO</p><div class="row-between"><div><h1>${title}</h1><p class="lead">${desc}</p></div><div class="row">${actions}</div></div></div>`}
function section(id,html){return `<section class="section" data-od-id="${id}"><div class="container">${html}</div></section>`}
function tabs(items,active){return `<div class="tabs" aria-label="Seções">${items.map(([id,label])=>`<button aria-pressed="${id===active}" data-action="tab:${id}">${label}</button>`).join('')}</div>`}
function table(headers,rows){return `<table class="ds-table"><thead><tr>${headers.map(h=>`<th scope="col">${h}</th>`).join('')}</tr></thead><tbody>${rows.length?rows.map(r=>`<tr>${r.map((c,i)=>`<td data-label="${headers[i]}">${c}</td>`).join('')}</tr>`).join(''):`<tr><td colspan="${headers.length}">Nenhum registro encontrado.</td></tr>`}</tbody></table>`}
function pager(totalItems){const pages=Math.max(1,Math.ceil(totalItems/8));return `<div class="pagination"><span>${totalItems} registros · Página ${pageNum} de ${pages}</span><div class="row">${btn('Anterior','page:-1',false,pageNum===1)}${btn('Próxima','page:1',false,pageNum>=pages)}</div></div>`}
function notice(text){return `<div class="notice" role="status">${text}</div>`}
function stateBox(kind,title,body,action=''){return `<div class="state-box" data-od-id="state-${kind}"><div class="state-mark" aria-hidden="true">${kind==='error'?'!':kind==='success'?'✓':kind==='empty'?'—':'…'}</div><h2>${title}</h2><p class="muted small">${body}</p>${action}</div>`}
function simulated(){const name=PAGES.find(p=>p.slug===PAGE)?.name; if(scenario==='carregando')return header(name,'Aguarde a consulta dos dados.')+`<div class="card" role="status" aria-busy="true"><span class="sr-only">Carregando dados</span>${Array.from({length:6},()=>'<div class="skeleton"></div>').join('')}</div>`;if(scenario==='erro'||scenario==='indisponivel')return header(name,'Seus dados não foram alterados.')+stateBox('error',scenario==='erro'?'Não foi possível carregar':'Serviço indisponível','Tente novamente. Se houver campos preenchidos, eles devem ser preservados.',btn('Tentar novamente','retry',true));if(scenario==='sem-acesso')return stateBox('error','Acesso não permitido','Você precisa participar deste projeto para consultar seus dados.',link('Voltar aos projetos','projetos',{},true));return ''}
function filterUI(){return `<form id="filters" class="filters" data-od-id="task-filters">${field('q','Buscar tarefa',filters.q||'','search')}${field('project','Projeto',filters.project||'','select',false,[['','Todos os projetos'],...db.projects.filter(accessible).map(p=>[p.id,p.name])])}${field('status','Situação',filters.status||'','select',false,[['','Todas'],...statuses])}${field('priority','Prioridade',filters.priority||'','select',false,[['','Todas'],...priorities])}<details><summary>Mais filtros e ordenação</summary><div class="filter-extra">${field('owner','Responsável',filters.owner||'','select',false,[['','Todos'],...db.users.map(u=>[u.id,u.name])])}${field('due','Prazo até',filters.due||'','date')}${field('sort','Ordenar por',filters.sort||'created','select',false,[['created','Criação'],['due','Prazo'],['priority','Prioridade'],['updated','Atualização']])}<label class="check"><input name="late" type="checkbox" ${filters.late?'checked':''}>Apenas atrasadas</label></div></details><div class="row"><button class="btn btn-secondary" type="submit">Aplicar filtros</button>${btn('Limpar','clear-filters')}</div></form>`}
function filtered(items){let r=items.filter(t=>(!filters.q||t.title.toLocaleLowerCase('pt-BR').includes(filters.q.toLocaleLowerCase('pt-BR')))&&(!filters.project||t.project==filters.project)&&(!filters.status||t.status===filters.status)&&(!filters.priority||t.priority===filters.priority)&&(!filters.owner||t.owner==filters.owner)&&(!filters.due||(t.due&&t.due<=filters.due))&&(!filters.late||late(t)));return r.sort((a,b)=>filters.sort==='priority'?priorities.indexOf(b.priority)-priorities.indexOf(a.priority):filters.sort==='due'?(a.due||'9999').localeCompare(b.due||'9999'):(b[filters.sort||'created']||0)-(a[filters.sort||'created']||0))}
function taskRow(t){const running=db.timer?.task===t.id;return `<article class="task-row" data-od-id="task-card-${t.id}"><div><a class="task-title" href="${url('detalhes-tarefa',{id:t.id})}">${esc(t.title)}</a><div class="task-meta"><span>${esc(projectName(t.project))}</span><span class="pill">${esc(t.status)}</span><span>${t.priority}</span><span class="num">${late(t)?'Atrasada · ':''}${date(t.due)}</span></div></div><div class="task-actions">${canEdit(t)&&t.status!=='Concluída'?btn(running?(db.timer.paused?'Retomar':'Pausar'):'Iniciar',running?'timer-pause':'timer-start:'+t.id):''}${canEdit(t)&&t.status!=='Concluída'?btn('Concluir','complete:'+t.id):''}</div></article>`}
function tasksPage(mine=false){const items=scenario==='vazio'?[]:filtered(visibleTasks().filter(t=>!mine||t.owner=='1'));let body=header(mine?'Minhas tarefas':'Tarefas',mine?'Organize o dia e registre o tempo de cada entrega.':'Consulte as tarefas dos projetos dos quais você participa.',link('Nova tarefa','nova-tarefa',{},true))+filterUI();if(!items.length)return body+stateBox('empty',scenario==='vazio'?'Nenhuma tarefa por aqui':'Nenhuma tarefa encontrada',mine?'As tarefas atribuídas a você aparecem aqui.':'Altere os filtros ou cadastre uma tarefa em um projeto.',btn('Limpar filtros','clear-filters'));if(mine){let used=new Set();const groups=[['Em execução',t=>db.timer?.task===t.id||t.status==='Em andamento'],['Atrasadas',late],['Para hoje',t=>t.due===localDate()&&t.status!=='Concluída'],['Próximas',t=>t.due>localDate()&&t.status!=='Concluída'],['Sem prazo',t=>!t.due&&t.status!=='Concluída'],['Concluídas recentemente',t=>t.status==='Concluída']];return body+groups.map(([label,test])=>{let arr=items.filter(t=>!used.has(t.id)&&test(t));arr.forEach(t=>used.add(t.id));return `<div class="group-head"><h2>${label}</h2><span class="count">${arr.length}</span></div>${arr.length?arr.map(taskRow).join(''):'<div class="empty-inline">Nenhuma tarefa nesta seção.</div>'}`}).join('')}return body+table(['Tarefa','Situação','Responsável','Prazo','Prioridade'],items.slice((pageNum-1)*8,pageNum*8).map(t=>[`<a href="${url('detalhes-tarefa',{id:t.id})}">${esc(t.title)}</a>`,`<span class="pill">${t.status}</span>`,esc(userName(t.owner)),`${late(t)?'Atrasada · ':''}${date(t.due)}`,t.priority]))+pager(items.length)}
function dashboard(){const ts=scenario==='vazio'?[]:visibleTasks(), ps=scenario==='vazio'?[]:db.projects.filter(p=>accessible(p)&&!p.archived), completed=ts.filter(t=>t.status==='Concluída'&&(!filters.from||new Date(t.completed).toLocaleDateString('sv-SE')>=filters.from)&&(!filters.until||new Date(t.completed).toLocaleDateString('sv-SE')<=filters.until));let h=header('Painel',role==='membro'?'Acompanhe seus projetos e acesse seu trabalho.':'Acompanhe progresso, atrasos e esforço da equipe.',link('Minhas tarefas','minhas-tarefas'));if(!ts.length)return h+stateBox('empty','Seu painel começa com um projeto','Os indicadores aparecem conforme projetos, tarefas e horas são registrados.',role!=='membro'?link('Criar projeto','novo-projeto',{},true):link('Ver meus projetos','projetos',{},true));if(role==='membro')return h+notice('Visão de membro: os indicadores gerenciais são restritos à gestão do projeto.')+projectsCards(ps);return h+`<form id="period" class="row" style="margin-bottom:20px">${field('from','Conclusões a partir de',filters.from||shift(-30),'date')}${field('until','Até',filters.until||localDate(),'date')}<button class="btn btn-secondary">Aplicar período</button></form><div class="grid-4">${[['Projetos ativos',ps.length],['Tarefas atrasadas',ts.filter(late).length],['Concluídas no período',completed.length],['Horas realizadas',duration(db.entries.filter(e=>ts.some(t=>t.id===e.task)).reduce((n,e)=>n+e.duration,0))]].map(([label,n])=>`<div class="metric"><span>${label}</span><strong>${n}</strong><span class="muted">Dados ilustrativos</span></div>`).join('')}</div><div class="grid-2" style="margin-top:20px"><div class="card"><h2>Tarefas por situação</h2>${statuses.map(s=>{let n=ts.filter(t=>t.status===s).length;return `<div class="chart-row"><div class="row-between"><span>${s}</span><span class="num">${n}</span></div><div class="bar" aria-label="${s}: ${n}"><span style="width:${n/ts.length*100}%"></span></div></div>`}).join('')}</div><div class="card"><h2>Esforço da equipe</h2><p class="small muted">Estimado: <span class="num">${duration(ts.reduce((n,t)=>n+Number(t.estimate||0),0))}</span></p>${db.users.map(u=>{let n=db.entries.filter(e=>e.user===u.id&&ts.some(t=>t.id===e.task)).reduce((a,e)=>a+e.duration,0);return `<div class="chart-row"><div class="row-between"><span>${esc(u.name)}</span><span class="num">${duration(n)}</span></div><div class="bar"><span style="width:${Math.min(100,n/Math.max(1,ts.reduce((a,t)=>a+Number(t.estimate||0),0))*100)}%"></span></div></div>`}).join('')}</div></div><div class="group-head"><h2>Progresso dos projetos</h2>${link('Ver projetos','projetos')}</div>${projectsCards(ps)}`}
function projectsCards(ps){return `<div class="grid-2">${ps.map(p=>{const ts=visibleTasks().filter(t=>t.project===p.id), done=ts.filter(t=>t.status==='Concluída').length, progress=ts.length?Math.round(done/ts.length*100):0;return `<article class="card" data-od-id="project-card-${p.id}"><div class="row-between"><span class="meta">${esc(p.code)}</span><span class="pill">${p.archived?'Arquivado':'Ativo'}</span></div><h2 style="margin:15px 0 8px"><a href="${url('detalhes-projeto',{projeto:p.id})}">${esc(p.name)}</a></h2><p class="small muted">Gestor: ${esc(userName(p.manager))}</p><div class="row-between small"><span>${done} de ${ts.length} tarefas concluídas</span><span class="num">${progress}%</span></div><div class="bar"><span style="width:${progress}%"></span></div><div class="row-between small" style="margin-top:16px"><span>Prazo: ${date(p.due)}</span><span class="num">${duration(ts.reduce((n,t)=>n+total(t),0))} realizadas</span></div><div style="margin-top:16px">${link('Abrir projeto','detalhes-projeto',{projeto:p.id})}</div></article>`}).join('')}</div>`}
function projectsPage(){let ps=scenario==='vazio'?[]:db.projects.filter(p=>accessible(p)&&(!filters.projectState||String(p.archived)===filters.projectState));return header('Projetos','Projetos dos quais você participa.',role!=='membro'?link('Novo projeto','novo-projeto',{},true):'')+`<div style="max-width:260px;margin-bottom:20px">${field('projectState','Exibir',filters.projectState||'','select',false,[['','Todos'],['false','Ativos'],['true','Arquivados']])}</div>`+(ps.length?projectsCards(ps.slice((pageNum-1)*8,pageNum*8))+pager(ps.length):stateBox('empty','Nenhum projeto disponível','Você verá aqui os projetos dos quais participa.'))}
function projectPage(){let p=project();if(!accessible(p))return stateBox('error','Acesso não permitido','Você não participa deste projeto.');const readOnly=p.archived||scenario==='arquivado';let h=header(esc(p.name),'Resumo, participantes, tarefas e horas do projeto.',canManage(p)?link('Editar projeto','editar-projeto',{projeto:p.id}):'');h+=readOnly?notice('Projeto arquivado · Apenas consulta. '+(canManage(p)?btn('Reativar projeto','project-reactivate:'+p.id):'')):'';h+=tabs([['resumo','Resumo'],['membros','Participantes'],['tarefas','Tarefas'],['horas','Horas']],tab);if(tab==='membros')return h+table(['Participante','Papel'],p.members.map(m=>[esc(userName(m.id)),role==='membro'&&m.id===1?'Membro':m.role]));if(tab==='tarefas')return h+`<div class="row">${link('Abrir quadro','quadro-projeto',{projeto:p.id})}${!readOnly?link('Nova tarefa','nova-tarefa',{projeto:p.id},true):''}</div>`+visibleTasks().filter(t=>t.project===p.id).map(taskRow).join('');if(tab==='horas')return h+entriesList(db.entries.filter(e=>db.tasks.find(t=>t.id===e.task)?.project===p.id));const ts=visibleTasks().filter(t=>t.project===p.id);return h+`<div class="grid-2-1"><div class="card"><h2>Sobre o projeto</h2><p>${esc(p.description)||'Sem descrição.'}</p><dl class="detail-list"><div><dt>Identificador</dt><dd>${esc(p.code)}</dd></div><div><dt>Gestor</dt><dd>${esc(userName(p.manager))}</dd></div><div><dt>Início</dt><dd>${date(p.start)}</dd></div><div><dt>Prazo</dt><dd>${date(p.due)}</dd></div></dl><hr class="rule"><div class="row">${link('Abrir quadro','quadro-projeto',{projeto:p.id})}${link('Ver tarefas','tarefas',{projeto:p.id})}</div></div><div class="card"><h2>Andamento</h2><p class="num">${ts.filter(t=>t.status==='Concluída').length} / ${ts.length} concluídas</p><div class="bar"><span style="width:${ts.length?ts.filter(t=>t.status==='Concluída').length/ts.length*100:0}%"></span></div><p class="small">${ts.filter(late).length} tarefas atrasadas</p><p class="small">Estimadas: ${duration(ts.reduce((n,t)=>n+Number(t.estimate||0),0))}<br>Realizadas: ${duration(ts.reduce((n,t)=>n+total(t),0))}</p></div></div>`}
function projectForm(edit=false){const p=edit?project():{name:'',code:'',description:'',manager:'1',start:'',due:'',members:[{id:1,role:'Gestor'}]};if(role==='membro')return stateBox('error','Ação restrita ao gestor','Você pode consultar os projetos dos quais participa.',link('Voltar aos projetos','projetos'));if(edit&&p.archived)return header('Editar projeto','Projeto arquivado')+notice('Reative o projeto para editar seus dados.')+btn('Reativar projeto','project-reactivate:'+p.id,true);return header(edit?'Editar projeto':'Novo projeto','Defina os dados do projeto e as pessoas participantes.')+`<form id="project-form" class="card form-card"><div class="form-grid">${field('name','Nome',p.name,'text',true)}${field('code','Identificador',p.code,'text',true)}<div class="span-2">${field('description','Descrição',p.description,'textarea')}</div>${field('manager','Gestor',p.manager,'select',true,db.users.filter(u=>u.active).map(u=>[u.id,u.name]))}${field('start','Data inicial',p.start,'date')}${field('due','Prazo',p.due,'date')}</div><hr class="rule"><h2>Participantes</h2><p class="small muted">O gestor participa obrigatoriamente do projeto.</p>${db.users.filter(u=>u.active).map(u=>`<div class="user-line"><label class="check"><input type="checkbox" name="member-${u.id}" ${p.members.some(m=>m.id===u.id)?'checked':''}>${esc(u.name)}</label><select class="input" name="role-${u.id}" aria-label="Papel de ${esc(u.name)}"><option ${p.members.find(m=>m.id===u.id)?.role!=='Gestor'?'selected':''}>Membro</option><option ${p.members.find(m=>m.id===u.id)?.role==='Gestor'?'selected':''}>Gestor</option></select></div>`).join('')}<div class="form-error" role="alert"></div><div class="form-actions">${link('Cancelar','projetos')}<button class="btn btn-primary">${edit?'Salvar alterações':'Criar projeto'}</button></div>${edit?`<hr class="rule"><h3>Arquivamento</h3><p class="small muted">O projeto ficará disponível apenas para consulta.</p>${btn('Arquivar projeto','project-archive:'+p.id)}`:''}</form>`}
function taskForm(edit=false){const t=edit?task():{title:'',project:Number(qs.get('projeto')||1),status:'Pendente',priority:'Média',owner:'',description:'',start:'',due:'',estimate:''};if(edit&&!canEdit(t))return stateBox('error','Tarefa disponível apenas para consulta','Somente o gestor ou responsável pode atualizar esta tarefa em um projeto ativo.',link('Ver tarefa','detalhes-tarefa',{id:t.id}));let p=db.projects.find(p=>p.id==t.project);return header(edit?'Editar tarefa':'Nova tarefa','Descreva a entrega, defina o responsável e planeje o prazo.')+`<form id="task-form" class="card form-card"><div class="form-grid">${field('project','Projeto',t.project,'select',true,db.projects.filter(p=>accessible(p)&&!p.archived).map(p=>[p.id,p.name]))}${field('title','Título',t.title,'text',true)}<div class="span-2">${field('description','Descrição',t.description,'textarea')}</div>${field('status','Situação',t.status,'select',true,edit?[t.status,...transitions[t.status]]:['Pendente'])}${field('priority','Prioridade',t.priority,'select',true,priorities)}${field('owner','Responsável',t.owner,'select',false,[['','Sem responsável'],...p.members.map(m=>[m.id,userName(m.id)])])}<div class="field"><label>Autor</label><div class="input">${esc(userName(edit?t.author:1))} · automático</div></div>${field('start','Data inicial',t.start,'date')}${field('due','Prazo',t.due,'date')}${field('estimate','Estimativa em minutos',t.estimate,'number')}</div><p class="small muted">A conclusão registra a data automaticamente. Reabrir remove essa data.</p><div class="form-error" role="alert"></div><div class="form-actions">${link('Cancelar',edit?'detalhes-tarefa':'tarefas',edit?{id:t.id}:{})}<button class="btn btn-primary">${edit?'Salvar alterações':'Criar tarefa'}</button></div></form>`}
function taskDetail(){const t=task();if(!accessible(db.projects.find(p=>p.id==t.project)))return stateBox('error','Acesso não permitido','Você não participa do projeto desta tarefa.');let edit=canEdit(t);let h=header(esc(t.title),`${esc(projectName(t.project))} · Tarefa ${t.id}`,edit?link('Editar tarefa','editar-tarefa',{id:t.id}):'');h+=`<div class="grid-2-1"><div><div class="card"><div class="row-between"><span class="pill">${t.status}</span><span class="pill">Prioridade ${t.priority}</span></div><p>${esc(t.description)||'Sem descrição.'}</p><div class="row">${edit&&t.status!=='Concluída'?btn(db.timer?.task===t.id?'Ver cronômetro':'Iniciar tarefa',db.timer?.task===t.id?'timer-focus':'timer-start:'+t.id,true):''}${edit?field('task-status','Alterar situação',t.status,'select',false,[t.status,...transitions[t.status]]):''}</div></div>${tabs([['resumo','Comentários'],['horas','Horas'],['historico','Histórico']],tab)}<div role="tabpanel">${tab==='horas'?entriesList(db.entries.filter(e=>e.task===t.id))+(edit?btn('Registrar horas','entry-new:'+t.id):''):tab==='historico'?`<ol class="timeline">${db.events.filter(e=>e.task===t.id).sort((a,b)=>a.date-b.date).map(e=>`<li><span class="meta">${stamp(e.date)} · ${esc(userName(e.user))}</span><p><strong>${esc(e.type)}</strong></p><p>${esc(e.before)} → ${esc(e.after)}</p></li>`).join('')}</ol>`:comments(t,edit)}</div></div><aside class="card"><h2>Detalhes</h2><dl class="detail-list" style="margin-top:20px">${[['Responsável',userName(t.owner)],['Autor',userName(t.author)],['Início',date(t.start)],['Prazo',date(t.due)],['Estimativa',duration(Number(t.estimate||0))],['Realizadas',duration(total(t))],['Conclusão',t.completed?stamp(t.completed):'Ainda não concluída']].map(([l,v])=>`<div><dt>${l}</dt><dd>${esc(v)}</dd></div>`).join('')}</dl>${late(t)?'<p class="pill" style="margin-top:20px">Atrasada</p>':''}${edit?`<hr class="rule">${btn('Arquivar tarefa','task-archive:'+t.id)}`:''}</aside></div>`;return h}
function comments(t,edit){return db.comments.filter(c=>c.task===t.id).sort((a,b)=>a.date-b.date).map(c=>`<article class="comment" data-od-id="comment-${c.id}"><div class="row-between"><strong>${esc(userName(c.user))}</strong><span class="meta">${stamp(c.date)}</span></div><p>${esc(c.text)}</p>${c.user===1&&edit?`<div class="row">${btn('Editar','comment-edit:'+c.id)}${btn('Excluir','comment-delete:'+c.id)}</div>`:''}</article>`).join('')+(edit?`<form id="comment-form" style="margin-top:20px">${field('comment','Adicionar comentário','','textarea',true)}<p class="small muted">Quebras de linha são preservadas.</p><button class="btn btn-secondary" style="margin-top:12px">Publicar comentário</button></form>`:notice('Consulta disponível. Atualizações são restritas ao responsável e ao gestor.'))}
function board(){const p=project(),ts=scenario==='vazio'?[]:visibleTasks().filter(t=>t.project===p.id);return header('Quadro do projeto',esc(p.name)+' · Altere a situação diretamente no card.',!p.archived?link('Nova tarefa','nova-tarefa',{projeto:p.id},true):'')+(p.archived?notice('Projeto arquivado · Apenas consulta.'):'')+`<div class="board">${statuses.map(s=>`<div class="board-column" data-od-id="column-${statuses.indexOf(s)}"><div class="row-between"><h2>${s}</h2><span class="count">${ts.filter(t=>t.status===s).length}</span></div>${ts.filter(t=>t.status===s).map(t=>`<article class="board-card" data-od-id="board-task-${t.id}"><a class="task-title" href="${url('detalhes-tarefa',{id:t.id})}">${esc(t.title)}</a><div class="task-meta"><span>${esc(userName(t.owner))}</span><span>${t.priority}</span><span>${late(t)?'Atrasada · ':''}${date(t.due)}</span><span>Est. ${duration(t.estimate)}</span><span>Real. ${duration(total(t))}</span></div>${canEdit(t)?`<label class="sr-only" for="status-${t.id}">Situação de ${esc(t.title)}</label><select class="input" id="status-${t.id}" data-task-status="${t.id}">${[s,...transitions[s]].map(st=>`<option>${st}</option>`).join('')}</select>`:''}</article>`).join('')||'<p class="empty-inline">Nenhuma tarefa.</p>'}</div>`).join('')}</div>`}
function entriesList(entries){return table(['Tarefa / pessoa','Data','Duração / origem','Ações'],entries.map(e=>{const t=db.tasks.find(t=>t.id===e.task),p=db.projects.find(p=>p.id===t?.project);return [`<div><a href="${url('detalhes-tarefa',{id:e.task})}">${esc(t?.title)}</a><div class="small muted">${esc(userName(e.user))}</div>${e.description?`<p class="small">${esc(e.description)}</p>`:''}</div>`,date(e.date),`<span class="num">${duration(e.duration)}</span><br><span class="small">${e.source}</span>`,e.user===1&&!p?.archived?`<div class="row">${btn('Editar','entry-edit:'+e.id)}${btn('Excluir','entry-delete:'+e.id)}</div>`:'Apenas consulta']}))}
function entriesPage(){let es=scenario==='vazio'?[]:db.entries.filter(e=>visibleTasks().some(t=>t.id===e.task)&&(!filters.project||db.tasks.find(t=>t.id===e.task)?.project==filters.project)&&(!filters.owner||e.user==filters.owner)&&(!filters.from||e.date>=filters.from)&&(!filters.until||e.date<=filters.until));return header('Apontamentos','Consulte as horas registradas e adicione tempo manualmente.',btn('Registrar horas','entry-new',true))+`<form id="entries-filter" class="filters">${field('project','Projeto',filters.project||'','select',false,[['','Todos'],...db.projects.filter(accessible).map(p=>[p.id,p.name])])}${field('owner','Pessoa',filters.owner||'','select',false,[['','Todas'],...db.users.map(u=>[u.id,u.name])])}${field('from','De',filters.from||'','date')}${field('until','Até',filters.until||'','date')}<button class="btn btn-secondary">Aplicar filtros</button></form>`+(es.length?`<p class="small">Total do período: <strong class="num">${duration(es.reduce((n,e)=>n+e.duration,0))}</strong></p>`+entriesList(es.slice((pageNum-1)*8,pageNum*8))+pager(es.length):stateBox('empty','Nenhuma hora registrada','Inicie um cronômetro em uma tarefa ou registre uma duração manualmente.'))}
function usersPage(){if(role!=='admin')return stateBox('error','Área exclusiva do administrador','A administração de usuários exige acesso administrativo.',link('Voltar ao painel','painel'));const us=scenario==='vazio'?[]:db.users;return header('Usuários','Gerencie cadastros, acesso administrativo e situação.',btn('Cadastrar usuário','user-new',true))+table(['Nome / e-mail','Estado','Acesso','Ações'],us.slice((pageNum-1)*8,pageNum*8).map(u=>[`<strong>${esc(u.name)}</strong><div class="small muted">${esc(u.email)}</div>`,u.active?'Ativo':'Inativo',u.admin?'Administrador':'Padrão',`<div class="row">${btn('Editar','user-edit:'+u.id)}${btn('Projetos','user-projects:'+u.id)}</div>`]))+pager(us.length)}
function profile(){const u=db.users[0];return header('Perfil','Atualize seus dados pessoais e sua senha.')+`<form id="profile-form" class="card form-card"><h2>Dados pessoais</h2><div class="form-grid" style="margin-top:20px">${field('name','Nome',u.name,'text',true)}${field('email','E-mail',u.email,'email',true)}</div><hr class="rule"><h2>Alterar senha</h2><p class="small muted">Preencha os campos abaixo somente se quiser alterar sua senha.</p><div class="form-grid">${field('currentPassword','Senha atual','','password')}${field('password','Nova senha','','password')}${field('confirmPassword','Confirmar nova senha','','password')}</div><div class="form-error" role="alert"></div><div class="form-actions"><button class="btn btn-primary">Salvar alterações</button></div></form>`}
function auth(){let recovery=PAGE==='recuperar-senha',reset=qs.get('etapa')==='redefinir';let title=recovery?(reset?'Redefinir senha':'Recuperar senha'):'Entrar';return `<main class="auth" data-od-id="authentication"><div class="app-brand"><span class="brand-mark">P</span>Gerenciador de projetos</div><h1>${title}</h1><p class="muted">${recovery?(reset?'Defina uma nova senha para acessar sua conta.':'Informe o e-mail associado à sua conta.'):'Acesse seus projetos e organize seu trabalho.'}</p>${scenario==='inativo'?notice('Usuário inativo. Solicite a reativação ao administrador.'):scenario==='erro'?notice(recovery?'O link é inválido ou expirou. Solicite uma nova recuperação.':'E-mail ou senha inválidos. Confira os dados e tente novamente.'):''}<form id="auth-form">${reset?field('password','Nova senha','','password',true)+field('confirmPassword','Confirmar nova senha','','password',true):field('email','E-mail','','email',true)+(recovery?'':field('password','Senha','','password',true))}<div class="form-error" role="alert"></div><button class="btn btn-primary">${recovery?(reset?'Salvar nova senha':'Enviar instruções'):'Entrar'}</button></form><a class="auth-link text-link" href="${url(recovery?'login':'recuperar-senha')}">${recovery?'Voltar ao login':'Esqueci minha senha'}</a><p class="auth-foot">Wireframe de baixa fidelidade. A autenticação e o envio de e-mail são simulados; não use uma senha real.</p></main>`}
function shell(body){let nav=[['painel','Painel','▦'],['minhas-tarefas','Minhas tarefas','≡'],['projetos','Projetos','□'],['tarefas','Tarefas','☷'],['apontamentos','Apontamentos','◷'],...(role==='admin'?[['usuarios','Usuários','○']]:[])];return `<div class="app-shell"><aside class="app-sidebar" data-od-id="app-sidebar"><div class="app-brand"><span class="brand-mark">P</span><span>Gerenciador<br>de projetos</span></div><button class="btn btn-secondary mobile-menu" data-action="menu-close">Fechar menu</button><nav class="app-nav" aria-label="Navegação principal">${nav.map(([p,n,i])=>`<a href="${url(p)}" ${p===PAGE?'aria-current="page"':''}><span class="nav-symbol" aria-hidden="true">${i}</span>${n}</a>`).join('')}</nav><div class="sidebar-foot"><a class="row" href="${url('perfil')}"><span class="avatar">PE</span><span class="small">${esc(db.users[0].name)}<br><span class="muted">${role==='admin'?'Administrador':role==='membro'?'Membro':'Gestor'}</span></span></a></div></aside><div class="workspace"><header class="app-top" data-od-id="app-topbar"><div class="row"><button class="btn btn-secondary mobile-menu" data-action="menu">Menu</button><span class="meta">Área de trabalho / ${PAGES.find(p=>p.slug===PAGE)?.name||'Projeto'}</span></div><a class="small" href="${url('login')}">Sair</a></header><div class="sample-strip">WIREFRAME · Dados ilustrativos · Sem identidade visual definitiva</div><div id="global-timer"></div><main id="content" class="page-body">${section('screen-'+PAGE,body)}</main></div></div>`}
const renderers={'painel':dashboard,'minhas-tarefas':()=>tasksPage(true),'tarefas':()=>tasksPage(),'projetos':projectsPage,'detalhes-projeto':projectPage,'novo-projeto':()=>projectForm(),'editar-projeto':()=>projectForm(true),'nova-tarefa':()=>taskForm(),'editar-tarefa':()=>taskForm(true),'detalhes-tarefa':taskDetail,'quadro-projeto':board,'apontamentos':entriesPage,'usuarios':usersPage,'perfil':profile};
function render(){document.body.innerHTML=(['login','recuperar-senha'].includes(PAGE)?auth():shell(simulated()||renderers[PAGE]()))+'<dialog id="dialog" aria-labelledby="dialog-title"></dialog><div id="toast" class="toast" role="status" aria-live="polite"></div>';renderTimer();annotate();if(scenario==='sucesso')toast('Operação concluída. Os dados foram salvos — estado ilustrativo.');if(scenario==='vazio'&&['detalhes-tarefa','detalhes-projeto'].includes(PAGE))$('#content').innerHTML=header(PAGES.find(p=>p.slug===PAGE).name,'')+stateBox('empty','Nenhum registro para exibir','Volte à listagem e escolha outro registro.',link('Voltar',PAGE==='detalhes-tarefa'?'tarefas':'projetos'));}
function annotate(){document.querySelectorAll('h1,h2,h3,button,input,textarea,select,a,article').forEach((el,i)=>{if(!el.dataset.odId)el.dataset.odId=PAGE+'-'+el.tagName.toLowerCase()+'-'+i})}
function toast(msg){const e=$('#toast');if(e){e.textContent=msg;clearTimeout(window.toastTimeout);window.toastTimeout=setTimeout(()=>e.textContent='',5500)}}
let restoreFocus=null;
function modal(title,body,submitLabel='',kind=''){restoreFocus=document.activeElement;const d=$('#dialog');d.innerHTML=`<div class="row-between"><h2 id="dialog-title">${title}</h2>${btn('Fechar','modal-close')}</div>${submitLabel?`<form id="modal-form" data-kind="${kind}">${body}<div class="form-error" role="alert"></div><div class="form-actions">${btn('Cancelar','modal-close')}<button class="btn btn-primary">${submitLabel}</button></div></form>`:body}`;d.dataset.component='x-ui.modal';d.showModal();annotate();d.addEventListener('close',()=>restoreFocus?.focus(),{once:true})}
function confirmAction(title,body,action){modal(title,`<p>${body}</p><div class="form-actions">${btn('Cancelar','modal-close')}${btn('Confirmar',action,true)}</div>`)}
function event(t,type,before,after){db.events.push({id:uid(),task:t.id,user:1,date:Date.now(),type,before,after});t.updated=Date.now()}
function changeStatus(t,next){if(!canEdit(t)||!transitions[t.status].includes(next))return toast('Esta alteração de situação não é permitida.');if(next==='Concluída'&&db.timer?.task===t.id)return modal('Finalize a execução primeiro',`<p>Registre ou descarte o tempo antes de concluir esta tarefa.</p><div class="row">${btn('Finalizar cronômetro','timer-finish',true)}${btn('Descartar execução','timer-discard')}</div>`);const old=t.status;t.status=next;t.completed=next==='Concluída'?Date.now():null;event(t,next==='Concluída'?'Conclusão':old==='Concluída'?'Reabertura':'Situação',old,next);save();render();toast('Situação atualizada.');}
function elapsed(){return db.timer?db.timer.accumulated+(db.timer.paused?0:Date.now()-db.timer.started):0}
function renderTimer(){const el=$('#global-timer');if(!el)return;if(!db.timer){el.innerHTML='';return}const t=db.tasks.find(t=>t.id===db.timer.task);el.innerHTML=`<div class="timer" data-od-id="global-timer"><div class="row-between"><div><div class="eyebrow">${db.timer.paused?'EXECUÇÃO PAUSADA':'EM EXECUÇÃO'}</div><a class="timer-title text-link" href="${url('detalhes-tarefa',{id:t.id})}">${esc(t.title)}</a></div><div class="task-actions"><span id="timer-clock" class="num timer-time"></span>${btn(db.timer.paused?'Retomar':'Pausar','timer-pause')}${btn('Finalizar','timer-finish')}${btn('Descartar','timer-discard')}</div></div></div>`;tick();annotate()}
function tick(){const el=$('#timer-clock');if(el){let s=Math.floor(elapsed()/1000);el.textContent=String(Math.floor(s/3600)).padStart(2,'0')+':'+String(Math.floor(s/60)%60).padStart(2,'0')+':'+String(s%60).padStart(2,'0')}}
function startTimer(id){const t=db.tasks.find(t=>t.id==id);if(!canEdit(t)||t.status==='Concluída')return toast('Não é possível iniciar esta tarefa.');if(db.timer)return modal('Já existe uma execução aberta',`<p>Finalize ou descarte “${esc(db.tasks.find(t=>t.id===db.timer.task)?.title)}” antes de iniciar outra tarefa. Uma execução pausada também precisa ser resolvida.</p><div class="row">${btn('Finalizar execução atual','timer-finish',true)}${btn('Descartar execução','timer-discard')}</div>`);if(t.status!=='Em andamento'){event(t,'Situação',t.status,'Em andamento');t.status='Em andamento'}db.timer={task:t.id,started:Date.now(),accumulated:0,paused:false};save();render();toast('Cronômetro iniciado.');}
function entryModal(id,taskId){const e=db.entries.find(e=>e.id==id);const tasks=visibleTasks().filter(t=>!db.projects.find(p=>p.id===t.project).archived);modal(e?'Editar apontamento':'Registrar horas',field('task','Tarefa',e?.task||taskId||tasks[0]?.id,'select',true,tasks.map(t=>[t.id,t.title]))+field('date','Data',e?.date||localDate(),'date',true)+field('duration','Duração em minutos',e?.duration||'','number',true)+field('description','Descrição',e?.description||'','textarea')+`<p class="small muted">Pessoa: ${esc(userName(1))} · Origem: ${e?.source||'Manual'}</p>`,'Salvar apontamento','entry:'+ (id||''))}
function userModal(id){if(role!=='admin')return;const u=db.users.find(u=>u.id==id);modal(u?'Editar usuário':'Cadastrar usuário',field('name','Nome',u?.name||'','text',true)+field('email','E-mail',u?.email||'','email',true)+field('active','Estado',String(u?.active??true),'select',true,[['true','Ativo'],['false','Inativo']])+`<label class="check"><input type="checkbox" name="admin" ${u?.admin?'checked':''}>Acesso administrativo</label>`,'Salvar usuário','user:'+(id||''))}
document.addEventListener('click',e=>{const control=e.target.closest('[data-action]');if(!control||control.disabled)return;const [a,id,arg]=control.dataset.action.split(':');if(a==='navigation'){return modal('Navegar',`<nav class="destination-grid" aria-label="Outras áreas">${[['painel','Painel de gestão'],['tarefas','Todas as tarefas'],...(role==='admin'?[['usuarios','Usuários']]:[]),['perfil','Meu perfil'],['login','Sair']].map(([p,n])=>link(n,p)).join('')}</nav>`)}if(a==='menu'){$('.app-sidebar').classList.add('open');$('.app-sidebar .mobile-menu').focus();return}if(a==='menu-close'){$('.app-sidebar').classList.remove('open');$('.app-top .mobile-menu').focus();return}if(a==='modal-close')return $('#dialog').close();if(a==='retry'){qs.delete('estado');location.search=qs.toString();return}if(a==='tab'){tab=id;render();document.querySelector('[data-action="tab:'+id+'"]')?.focus();return}if(a==='page'){pageNum+=Number(id);render();return}if(a==='clear-filters'){filters={};sessionStorage.removeItem('mvp-filtros-'+PAGE);if(scenario==='vazio'){qs.delete('estado');location.search=qs.toString()}else render();return}if(a==='timer-start')return startTimer(id);if(a==='timer-focus')return toast('Use os controles da execução no topo da tela.');if(a==='timer-pause'){if(!db.timer)return;db.timer.accumulated=elapsed();db.timer.paused=!db.timer.paused;db.timer.started=Date.now();save();render();return}if(a==='timer-finish'){if(!db.timer)return;return modal('Finalizar cronômetro',`<p>O tempo será registrado na tarefa e o cronômetro será encerrado.</p><p class="num">Duração: ${duration(Math.max(1,Math.ceil(elapsed()/60000)))}</p>${field('description','Descrição do trabalho','','textarea')}<p class="small muted">Duração arredondada para o próximo minuto neste protótipo.</p>`,'Registrar e finalizar','timer')}if(a==='timer-discard')return confirmAction('Descartar execução?','O tempo desta execução não será apontado.','confirm-discard');if(a==='confirm-discard'){db.timer=null;save();render();return toast('Execução descartada.')}if(a==='complete')return changeStatus(db.tasks.find(t=>t.id==id),'Concluída');if(a==='entry-new'||a==='entry-edit')return entryModal(a==='entry-edit'?id:null,a==='entry-new'?id:null);if(a==='entry-delete')return confirmAction('Excluir apontamento?','A duração será removida das horas realizadas.','confirm-entry-delete:'+id);if(a==='confirm-entry-delete'){db.entries=db.entries.filter(e=>e.id!=id||e.user!==1);save();render();return toast('Apontamento excluído.')}if(a==='comment-edit'){const c=db.comments.find(c=>c.id==id);return modal('Editar comentário',field('text','Comentário',c.text,'textarea',true),'Salvar comentário','comment:'+id)}if(a==='comment-delete')return confirmAction('Excluir comentário?','O comentário será removido da tarefa.','confirm-comment-delete:'+id);if(a==='confirm-comment-delete'){db.comments=db.comments.filter(c=>c.id!=id||c.user!==1);save();render();return toast('Comentário excluído.')}if(a==='task-archive')return confirmAction('Arquivar tarefa?','A tarefa sai das listas de trabalho. O escopo não define reativação de tarefas.','confirm-task-archive:'+id);if(a==='confirm-task-archive'){const t=db.tasks.find(t=>t.id==id);if(db.timer?.task==id)return toast('Finalize ou descarte a execução antes de arquivar.');if(canEdit(t)){t.archived=true;save();go('tarefas')}return}if(a==='project-archive')return confirmAction('Arquivar projeto?','Tarefas, comentários e horas ficam disponíveis somente para consulta.','confirm-project-archive:'+id);if(a==='confirm-project-archive'){const p=db.projects.find(p=>p.id==id);if(db.timer&&db.tasks.find(t=>t.id===db.timer.task)?.project===p.id)return toast('Finalize a execução deste projeto antes de arquivá-lo.');if(canManage(p)){p.archived=true;save();go('detalhes-projeto',{projeto:p.id})}return}if(a==='project-reactivate'){const p=db.projects.find(p=>p.id==id);if(canManage(p)){p.archived=false;save();qs.delete('estado');location.href=url('detalhes-projeto',{projeto:p.id})}return}if(a==='user-new'||a==='user-edit')return userModal(id);if(a==='user-projects'){const ps=db.projects.filter(p=>p.members.some(m=>m.id==id));return modal('Projetos do usuário',ps.length?table(['Projeto','Papel'],ps.map(p=>[esc(p.name),p.members.find(m=>m.id==id).role])):'<p>Este usuário não participa de projetos.</p>')}});
document.addEventListener('change',e=>{if(e.target.dataset.taskStatus)changeStatus(db.tasks.find(t=>t.id==e.target.dataset.taskStatus),e.target.value);if(e.target.name==='task-status')changeStatus(task(),e.target.value);if(e.target.name==='projectState'){filters.projectState=e.target.value;render()}if(e.target.name==='project'&&e.target.closest('#task-form')){const p=db.projects.find(p=>p.id==e.target.value);$('#field-owner').innerHTML='<option value="">Sem responsável</option>'+p.members.map(m=>`<option value="${m.id}">${esc(userName(m.id))}</option>`).join('')}});
function formError(form,message){const el=form.querySelector('.form-error');if(el){el.textContent=message;el.tabIndex=-1;el.focus()}else toast(message)}
document.addEventListener('submit',e=>{e.preventDefault();const f=e.target,d=Object.fromEntries(new FormData(f)),id=f.id; if(['filters','period','entries-filter'].includes(id)){filters=d;pageNum=1;sessionStorage.setItem('mvp-filtros-'+PAGE,JSON.stringify(filters));render();return}if(id==='auth-form'){if(scenario==='inativo')return formError(f,'Usuário inativo. Solicite a reativação ao administrador.');if(PAGE==='recuperar-senha'){if(qs.get('etapa')==='redefinir'){if(d.password!==d.confirmPassword)return formError(f,'As senhas não coincidem.');$('.auth').innerHTML=stateBox('success','Senha redefinida','Agora você pode entrar com sua nova senha.',link('Voltar ao login','login',{},true));return}$('.auth').innerHTML=stateBox('success','Confira seu e-mail','Se houver uma conta ativa para este e-mail, você receberá instruções para redefinir a senha.',link('Voltar ao login','login',{},true))+'<p class="auth-foot">Simulação: nenhum e-mail foi enviado.</p>';return}const u=db.users.find(u=>u.email.toLowerCase()===d.email.toLowerCase());if(u&&!u.active)return formError(f,'Usuário inativo. Solicite a reativação ao administrador.');go('painel');return}if(id==='project-form'){if(PAGE==='editar-projeto'){const current=project();const removed=current.members.filter(m=>m.id!=d.manager&&!d['member-'+m.id]);if(db.tasks.some(t=>t.project===current.id&&removed.some(m=>m.id==t.owner)))return formError(f,'Reatribua as tarefas antes de remover um participante responsável.');}if(d.start&&d.due&&d.due<d.start)return formError(f,'O prazo deve ser igual ou posterior à data inicial.');if(db.projects.some(p=>p.code===d.code&&(PAGE!=='editar-projeto'||p.id!==project().id)))return formError(f,'Este identificador já está em uso.');let p=PAGE==='editar-projeto'?project():{id:uid(),archived:false};Object.assign(p,{name:d.name.trim(),code:d.code.trim(),description:d.description,manager:d.manager,start:d.start,due:d.due,members:db.users.filter(u=>d['member-'+u.id]||u.id==d.manager).map(u=>({id:u.id,role:u.id==d.manager?'Gestor':d['role-'+u.id]}))});if(!p.members.some(m=>m.id===1))p.members.push({id:1,role:'Gestor'});if(PAGE!=='editar-projeto')db.projects.push(p);save();go('detalhes-projeto',{projeto:p.id,estado:'sucesso'});return}if(id==='task-form'){if(d.start&&d.due&&d.due<d.start)return formError(f,'O prazo deve ser igual ou posterior à data inicial.');const p=db.projects.find(p=>p.id==d.project);if(!p||p.archived||!accessible(p))return formError(f,'Selecione um projeto ativo do qual você participa.');if(d.owner&&!p.members.some(m=>m.id==d.owner))return formError(f,'O responsável deve participar do projeto.');let t=PAGE==='editar-tarefa'?task():{id:uid(),author:1,created:Date.now(),updated:Date.now(),completed:null,archived:false};if(PAGE==='editar-tarefa'){if(!canEdit(t))return formError(f,'Você não pode editar esta tarefa.');if(d.status!==t.status&&(!transitions[t.status].includes(d.status)||db.timer?.task===t.id))return formError(f,'Finalize a execução antes de alterar a situação.');[['status','Situação'],['owner','Responsável'],['priority','Prioridade'],['due','Prazo'],['estimate','Estimativa']].forEach(([k,l])=>{if(String(t[k]||'')!==String(d[k]||''))event(t,l,t[k]||'—',d[k]||'—')})}else event(t,'Criação da tarefa','—','Pendente');Object.assign(t,{...d,project:Number(d.project),estimate:Number(d.estimate||0),completed:d.status==='Concluída'?(t.completed||Date.now()):null,updated:Date.now()});if(PAGE!=='editar-tarefa')db.tasks.push(t);save();go('detalhes-tarefa',{id:t.id,estado:'sucesso'});return}if(id==='comment-form'){if(!d.comment.trim())return formError(f,'Escreva um comentário.');db.comments.push({id:uid(),task:task().id,user:1,date:Date.now(),text:d.comment});save();render();toast('Comentário publicado.');return}if(id==='profile-form'){if((d.password||d.confirmPassword||d.currentPassword)&&(!d.password||!d.currentPassword||d.password!==d.confirmPassword))return formError(f,'Informe a senha atual e confirme a nova senha corretamente.');Object.assign(db.users[0],{name:d.name,email:d.email});save();render();toast(d.password?'Dados salvos. Alteração de senha simulada.':'Perfil atualizado.');return}if(id==='modal-form'){const [kind,itemId]=f.dataset.kind.split(':');if(kind==='entry'){if(Number(d.duration)<=0||!d.task)return formError(f,'Informe uma tarefa e uma duração maior que zero.');const entry=db.entries.find(e=>e.id==itemId);if(entry&&entry.user!==1)return formError(f,'Você só pode editar seus apontamentos.');const values={task:Number(d.task),date:d.date,duration:Number(d.duration),description:d.description};if(entry)Object.assign(entry,values);else db.entries.push({id:uid(),user:1,source:'Manual',...values})}if(kind==='timer'){if(!db.timer)return;db.entries.push({id:uid(),task:db.timer.task,user:1,date:localDate(),duration:Math.max(1,Math.ceil(elapsed()/60000)),description:d.description,source:'Cronômetro'});db.timer=null}if(kind==='comment'){const c=db.comments.find(c=>c.id==itemId);if(c.user===1)c.text=d.text}if(kind==='user'){if(role!=='admin')return;if(db.users.some(u=>u.email===d.email&&u.id!=itemId))return formError(f,'Este e-mail já está cadastrado.');const u=db.users.find(u=>u.id==itemId);const vals={name:d.name,email:d.email,active:d.active==='true',admin:!!d.admin};if(u)Object.assign(u,vals);else db.users.push({id:uid(),...vals})}save();render();toast('Alterações salvas.')}});
if(qs.get('projeto')&&PAGE==='tarefas'&&!filters.project)filters.project=qs.get('projeto');
render();setInterval(tick,1000);window.addEventListener('storage',e=>{if(e.key===STORE&&e.newValue){try{db=JSON.parse(e.newValue);if(!$('#dialog')?.open&&!document.activeElement?.closest('form'))render();else renderTimer()}catch{}}});

document.addEventListener('keydown',e=>{if(e.key==='Escape')document.querySelector('.app-sidebar')?.classList.remove('open');const parent=e.target.closest('.tabs');if(parent&&['ArrowLeft','ArrowRight','Home','End'].includes(e.key)){const tabs=[...parent.querySelectorAll('button')],i=tabs.indexOf(e.target);if(i<0)return;e.preventDefault();const next=e.key==='Home'?0:e.key==='End'?tabs.length-1:(i+(e.key==='ArrowRight'?1:-1)+tabs.length)%tabs.length;tabs[next].click();}});


```
